# AI Architecture (Gemini)

- Model: `gemini-1.5-flash`
- Clinical advice: JSON schema validated (`diagnosis`, `recommendations`, `urgency`, `confidence`, `reasoning`)
- Intent extraction: `{ intent, entities, confidence }`
- `DEMO_MODE=true`: canned responses, no network
- All outputs labeled AI suggestion, not verified clinical fact
